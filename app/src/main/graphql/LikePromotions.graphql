mutation LikePromotionMutation($postId: ID!, $userId: ID!) {
  result: addToPromotionsLikeByUsers(myPromotionsPostId: $postId, usersLikedUserId: $userId) {
    usersLikedUser { id }, myPromotionsPost { id }
  }
}