mutation UnlikePromotionMutation($postId: ID!, $userId: ID!) {
  result: removeFromPromotionsLikeByUsers(myPromotionsPostId: $postId, usersLikedUserId: $userId) {
    usersLikedUser { id }, myPromotionsPost { id }
  }
}