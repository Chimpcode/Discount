<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.Entretenimiento" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/entretenimientoColor</item>
        <item name="colorPrimaryDark">@color/entretenimientoColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.Belleza" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bellezaColor</item>
        <item name="colorPrimaryDark">@color/bellezaColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.Bares" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/baresColor</item>
        <item name="colorPrimaryDark">@color/baresColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.Restaurants" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/restaurantsColor</item>
        <item name="colorPrimaryDark">@color/restaurantsColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.Servicios" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/serviciosColor</item>
        <item name="colorPrimaryDark">@color/serviciosColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.Varios" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/variosColor</item>
        <item name="colorPrimaryDark">@color/variosColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>


    <style name="AppTheme.TabLayout" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="tabSelectedTextColor">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <!--<item name="android:windowBackground">@color/primary</item>-->
    </style>

    <style name="AppTheme.ButtonColored" parent="Widget.AppCompat.Button.Colored">
        <!-- Customize your theme here. -->
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="background">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/nexa_bold</item>
    </style>

    <style name="AppTheme.ButtonBorderless" parent="Widget.AppCompat.Button.Borderless">
        <!-- Customize your theme here. -->
        <!--<item name="colorButtonNormal">@color/colorAccent</item>-->
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="FacebookLoginButton">
        <item name="android:textSize">16dp</item>
        <item name="android:background">@color/primary_dark</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

</resources>
